# karin integration manifest
# ---------------------------------------------------------------------------
# karin = master + these component branches, merged in the listed order.
# karin is a BRANCH, never a worktree (see rebuild-karin.sh).
#
# Rebuild:  ./rebuild-karin.sh
# Push:     git push --force-with-lease Hrovatin karin
#
# Blank lines and #-comments are ignored; inline "# ..." comments are stripped.
# ---------------------------------------------------------------------------

feature/issue-439-fork-from-current-state
vscode                                             # the full vscode branch: VS Code extension Phases 0-4b (PRs #14/#16/#18/#21/#23) + worktree isolation (#233) + parallel-mode (#4) + test coverage (#8), REBASED ONTO feature/issue-10-ask-mode (issue #10). Contains BOTH worktree-cleaned's domain AND ask-mode, so neither worktree-cleaned nor feature/issue-10-ask-mode is listed separately.
feature/issue-1-architecture-section-push-comments
feature/issue-6-mach6mini-skills
feature/focused-system-prompt                      # PR #19 — more focused default system prompt
feature/review-findings-user-impact                # PR #60 — AGENTS.md: require user-impact explanation in review findings
feature/karin-rebuild-skill                        # karin-rebuild skill: documents this integration-branch rebuild process (skills/karin-rebuild)
